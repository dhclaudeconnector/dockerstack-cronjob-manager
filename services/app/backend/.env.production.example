# Deprecated service-local template.
# For one-machine deployment use the shared root file instead:
#   ../.env.production.example
# Backend still supports SHARED_ENV_FILE=/absolute/path/to/.env if your process manager stores env elsewhere.
